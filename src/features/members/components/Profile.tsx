import { Id } from "../../../../convex/_generated/dataModel";

export const Profile = ({
    onClose,
    memberId,
} : {
    onClose: () => void;
    memberId: Id<"members">;
}) => {
    return (
        <></>
    )
}